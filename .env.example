# ResSuite Environment Variables

# Base API URL
VITE_BASE_URL=http://localhost:8000

# Assist Services
VITE_TRACE_ASSIST_URL=http://localhost:5001
VITE_TOIL_ASSIST_URL=http://localhost:5002
VITE_DASHBOARD_ASSIST_URL=http://localhost:5003
VITE_DRIFT_ASSIST_URL=http://localhost:5004

# DR Assist Configuration
# DR Assist APIs are available on the main ResSuite backend (port 8000)
# All /dr/* endpoints are handled by VITE_BASE_URL
# Port 9100: DR Score Backend (AI analysis engine - called internally by ResSuite)
VITE_DR_ASSIST_URL=http://localhost:9100

# Note: Port 9100 (DR Score Backend) is NOT called directly from frontend
# It's called internally by ResSuite Backend (8000)
